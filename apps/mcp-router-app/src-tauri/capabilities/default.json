{
  "identifier": "default",
  "description": "Default capabilities for the MCP Router app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "opener:default"
  ]
}
